import { View } from 'react-native';
import styled from 'styled-components';

export const MainView = styled(View)`
  height: 78%;
  position: absolute;
  top: 0;
  right: 0;
  left: 0;
  padding: 10px;
`;
